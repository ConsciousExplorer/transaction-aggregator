info:
  name: get-schema-by-id
  type: http
  seq: 4

http:
  method: GET
  url: "{{schema-registry}}/schemas/ids/:schema-id"
  params:
    - name: schema-id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
