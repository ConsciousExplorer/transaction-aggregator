info:
  name: get-subject-versions
  type: http
  seq: 3

http:
  method: GET
  url: "{{schema-registry}}/subjects/:subject-name/versions"
  params:
    - name: subject-name
      value: transactions.card-value
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
